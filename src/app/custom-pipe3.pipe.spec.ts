import { CustomPIPE3Pipe } from './custom-pipe3.pipe';

describe('CustomPIPE3Pipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPIPE3Pipe();
    expect(pipe).toBeTruthy();
  });
});
